{
  "name": "mandelbrot-landscape-explorer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Real-time WebGL Mandelbrot potential-landscape explorer (after Peitgen & Richter, 1985-86). Self-hosted static site, zero runtime dependencies.",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.1.3"
  }
}
